This fixture captures real 0007 (`skills/config/migrate/migrations/0007-unify-meta-corpus-frontmatter.sh`) against a small, fully base-field-valid two-work-item corpus with a `## References` section pointing at another real work item.

It does **not** exercise the `MIGRATION STALLED` / interactive-prompt path: both an existing-target reference (`work-item:0002`, resolvable) and a non-existent-target reference (`work-item:0099`) were tried empirically and both apply mechanically with no decision required — the latter is silently dropped (matching the plan's noted `0007-DIVERGE[reverse-orphan]` silent-drop behaviour for unresolvable targets), not treated as ambiguous.

Triggering the genuinely `ambiguous`-band case that requires a human decision needs a reference shape that is structurally ambiguous by construction (per `corpus::linkage::classify_band`), not merely an unresolved target. That is deeper 0007-specific domain knowledge than this phase needs to nail down — 0007's real port is Phase 8, which is itself blocked on work item 0195's self-validation obligation and far downstream of the phases (1–6, 9) this capture unblocks now. Revisit this fixture when Phase 8 actually starts: either extend it with a genuinely ambiguous reference and recapture, or add a second fixture (e.g. `0007-ambiguous/`) alongside this one.
