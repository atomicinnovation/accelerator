---
# Owned by meta/work/0074 Phase 5 / AC #2. Slug stem "ac2-coverage" MUST
# match across all 13 sibling fixtures under server/tests/fixtures/meta/.
# Carry NO cross-reference keys (work_item_id / work-item / ticket / parent
# / related / target) — they would pull a sibling into a declared relation.
# `id:` is the work-item identity key (not a cross-reference key): it anchors
# the cluster_key that the other doc-type siblings adopt via the slug bridge,
# so it must be present for them to cluster as inferred rows.
title: AC2 Coverage (work items)
id: "0099"
status: draft
---

AC #2 coverage fixture for work-items.
