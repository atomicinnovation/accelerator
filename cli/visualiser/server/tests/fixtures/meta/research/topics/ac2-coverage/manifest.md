---
# Owned by meta/work/0074 Phase 5 / AC #2. Slug stem "ac2-coverage" MUST
# match across the sibling fixtures (one per non-virtual doc type) under
# server/tests/fixtures/meta/. Slug derives from the parent directory name
# verbatim (the topic-research identity arm), so the directory carries no date
# prefix. Carry NO cross-reference keys (work_item_id / work-item / ticket /
# parent / related / target) — they would pull a sibling into a declared
# relation.
title: AC2 Coverage (topic research)
---

AC #2 coverage fixture for topic-research.
