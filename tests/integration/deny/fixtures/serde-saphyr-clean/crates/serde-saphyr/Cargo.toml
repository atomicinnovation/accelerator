# Local stand-in for the crates.io serde-saphyr, named identically so the ban
# matches on crate name with no network.
[package]
name = "serde-saphyr"
version = "0.0.29"
edition = "2021"
license = "MIT"
