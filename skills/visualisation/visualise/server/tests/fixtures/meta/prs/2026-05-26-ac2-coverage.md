---
# Owned by meta/work/0074 Phase 5 / AC #2. Slug stem "ac2-coverage" MUST
# match across all 12 sibling fixtures under server/tests/fixtures/meta/.
# Uses a date prefix (NOT a numeric PR id) so strip_prefix_date derives the
# shared "ac2-coverage" slug. Carry NO cross-reference keys.
title: AC2 Coverage (PR descriptions)
---

AC #2 coverage fixture for pr-descriptions.
